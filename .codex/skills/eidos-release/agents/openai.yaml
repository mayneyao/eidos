interface:
  display_name: "Eidos Release"
  short_description: "Plan and release Eidos product surfaces"
  default_prompt: "Use $eidos-release to determine which Eidos surfaces need shipping, then validate and publish each selected release or deployment with remote proof."
