---
title: 自定义新建标签页
description: 将默认的新建标签页替换为自定义 Block
sidebar:
  order: 2
---

import { Steps } from "@astrojs/starlight/components"

默认情况下，新建标签页显示欢迎界面。你可以使用 `"use newtab"` 指令将其替换为自定义 Block。

## 步骤

<Steps>

1. **创建 Block**

   前往 **扩展** → **区块** → **创建**。

2. **添加指令**

   在代码第一行添加 `"use newtab"`：

   ```tsx
   "use newtab"

   export function MyDashboard() {
     return (
       <div className="flex h-full items-center justify-center">
         <h1 className="text-2xl font-bold">Hello, Eidos!</h1>
       </div>
     )
   }
   ```

3. **在设置中启用**

   按 `Cmd/Ctrl + ,` 打开设置，然后前往 **空间设置** → **新建标签页**，选择你的 Block。

</Steps>

## 示例：跳转到今日日志

```tsx
"use newtab"

import React from "react"
import { useEidos } from "@eidos.space/react"

export function TodayView() {
  const eidos = useEidos()
  const today = new Date().toLocaleDateString("en-CA")

  const openToday = () => {
    eidos.currentSpace.navigate(`/journals/${today}`)
  }

  return (
    <div className="h-full flex flex-col items-center justify-center gap-4">
      <div className="text-4xl font-mono">{today}</div>
      <button
        onClick={openToday}
        className="px-4 py-2 bg-primary text-primary-foreground rounded-md hover:opacity-90"
      >
        打开今日日志
      </button>
    </div>
  )
}
```

## 相关链接

- [`use newtab` 指令](/extensions/block/#52-use-newtab-directive)
- [Extension API](/api-reference/extension/) - 构建扩展的 React Hooks
- [Space API](/api-reference/space/) - 导航和数据访问 API
