// Playground feature removed - this component is deprecated
// Keeping as empty export to avoid breaking imports

import type { ITreeNode } from "@/packages/core/types/ITreeNode"

interface NodeOpenInCursorProps {
  node: ITreeNode
}

export const NodeOpenInCursor = (_props: NodeOpenInCursorProps) => {
  // Playground feature removed
  return null
}
