## 问题修复

- **记录 Content 图片**：与 Eidos File 一起存储的图片现在可以在记录 Content 页面正确显示，包括 Markdown 和 HTML 图片元素中的相对路径。
- **Sync 连接设置**：尚未连接的空间现在也使用与已连接空间一致的紧凑 Sync 面板和账号菜单。保留首次上传检查，移除重复的账号和存储信息区域。
- **多选字段转换**：将包含 JSON 字符串数组（如 `["Work","Personal"]`）的文本字段转换为多选时，现在会生成独立选项，而不是把整个数组作为一个选项。普通文本仍保留为单个选项。
