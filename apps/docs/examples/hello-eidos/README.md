# Hello Eidos

This is the source-backed example used by the file-based extension Quickstart.
The runtime and `@eidos.space/extension-sdk` package are still a design preview,
so this example is validated structurally but is not compiled yet.
