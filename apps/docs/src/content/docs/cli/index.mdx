---
title: CLI and automation
description: Use readable terminal output or explicit JSON automation with the Eidos CLI.
sidebar:
  order: 1
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components"

`eidos` is an agent-first Rust CLI for the open Eidos File format. Most commands
read and modify files locally; Relay and Publish connect only when you choose
those hosted workflows explicitly.

<CardGrid>
  <LinkCard
    title="Install and create a file"
    description="Complete the first visual workflow in a few commands."
    href="/getting-started/cli/"
  />
  <LinkCard
    title="Automate safely"
    description="Use context, revision checks, atomic apply, and full validation."
    href="/cli/automation-workflow/"
  />
  <LinkCard
    title="Serve the local editor"
    description="Open the shared Eidos File UI for one path on localhost."
    href="/cli/serve/"
  />
  <LinkCard
    title="Publish a hosted copy"
    description="Upload immutable Versions to a stable, read-only URL."
    href="/cli/publish/"
  />
</CardGrid>

## Command model

The help output uses command-first syntax:

```bash
eidos inspect tracker.eidos
eidos context tracker.eidos Tasks --fields Title,Status --limit 50
eidos validate tracker.eidos --level full
```

File-first forms such as `eidos tracker.eidos inspect` are also accepted for
compatibility. New scripts should prefer command-first syntax so they match
`eidos --help`.

Commands print readable key-value sections and tables by default. Pass the
global `--json` flag for Agent and script workflows:

```bash
eidos --json context tracker.eidos Tasks --fields Title,Status --limit 50
```

In JSON mode, successful commands write one JSON document to stdout. Failed
commands write one JSON error document to stderr and return a nonzero exit
status. The JSON schema is the stable automation contract; human output may
evolve for readability.

## Supported responsibility

The CLI creates, inspects, queries, mutates, validates, and serves Eidos Files.
Publish accepts both Eidos Files and Markdown documents with local attachments.
The CLI does not otherwise manage ordinary documents, Eidos Lite Space
lifecycle, application RPC, local version history, or Sync. Use normal file
tools for ordinary files and Graft-aware workflows for history.

Raw SQLite writes are not a supported automation interface.
