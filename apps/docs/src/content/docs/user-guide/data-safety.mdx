---
title: Data location and safety
description: Know where your Eidos data lives and when writes become durable.
sidebar:
  order: 60
---

## Where data lives

| Host                           | Durable data                                | How writes reach it                                                          |
| ------------------------------ | ------------------------------------------- | ---------------------------------------------------------------------------- |
| Web Editor, direct-file mode   | The `.eidos` file you selected              | **Save** writes after browser permission and conflict checks                 |
| Web Editor, imported-copy mode | A file you save or download                 | The in-browser working copy is not the original; use **Save As** or download |
| CLI commands                   | The path passed to `eidos`                  | Successful mutations commit atomically to that file                          |
| CLI Serve                      | The path passed to `eidos serve`            | Each committed editor mutation writes through to the file                    |
| Eidos Lite                     | `.eidos` files in the ordinary Space folder | Runtime mutations write to those local files; history is a separate layer    |

No supported host requires uploading a file merely to query or edit it.

## Read the save state correctly

A successful cell commit means Runtime accepted the mutation. Whether that
mutation has reached the original file depends on the Host in the table above.
In particular, an in-browser working copy can be valid and fully editable while
still requiring **Save As** before it exists outside browser storage.

If the original file changed outside the current editor, stop and choose an
explicit reload, overwrite, or Save As path. Do not assume the most recently
visible window owns the newest complete state.

## Safe habits

1. Keep ordinary backups of important `.eidos` files or the whole Space.
2. Do not edit the same file through multiple writers at once. Close the other
   host if you see a busy, locked, or external-change warning.
3. Watch the titlebar save state before closing a browser tab.
4. Use **Save As** when write permission is unavailable or the original changed
   outside Eidos.
5. After automated writes, run full validation.

```bash
eidos important.eidos validate --level full
```

## Back up and test a restore

A useful backup contains the `.eidos` file and every ordinary Space asset you
expect to keep. Copy it while no writer is actively committing, then verify the
copy by opening it or running validation. A backup process that has never
restored a file is only an assumption.

CSV export is for exchange, not backup. It omits identities, View definitions,
Relations, Formulas, option metadata, and complete File entries.

## Attachment safety

A File field stores portable entry metadata. The referenced bytes may be inline,
relative to the Host's asset area, or available through an explicitly authorized
HTTPS resource. Backing up only the database file is insufficient when your
workflow uses separate relative assets; back up the whole Space layout.

Previewing an attachment is not the same as validating or trusting its content.
Open untrusted documents with the same care you would use outside Eidos.

## Raw SQLite access

It is reasonable to inspect an Eidos File with SQLite tools. Direct SQL writes
are not a supported editing boundary because they can bypass revision,
canonical value, relation, derived-field, and validation rules. Use Runtime or
the CLI for changes.
