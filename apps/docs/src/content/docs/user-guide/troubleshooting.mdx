---
title: Troubleshooting
description: Resolve common Eidos File, Web Editor, CLI, and save problems.
sidebar:
  order: 80
---

## A file will not open

Run a full validation and read the JSON error:

```bash
eidos problem.eidos validate --level full
```

Check that the path ends in `.eidos`, the file is actually SQLite, and it was
created by a compatible Eidos File implementation. Renaming an arbitrary
SQLite database does not convert it.

## Web edits did not reach the original

Look at the titlebar state. Imported-copy mode and denied write permission
require **Save As** or **Download a copy**. If the original changed outside the
editor, choose reload, explicit overwrite, or Save As rather than silently
replacing either version.

## The file is busy or locked

Close other Eidos, SQLite, preview, backup, or indexing processes that may hold
the file. Do not run Desktop development and Node tests against the same native
SQLite binary concurrently in this repository.

## `eidos serve` does not open a browser

Start without `--open`, then copy the printed loopback URL into a browser:

```bash
eidos serve example.eidos --port 8420
```

The server binds `127.0.0.1` by default. Check local firewall or proxy rules if
the URL is unreachable. For another device on a trusted private network,
restart it with `--lan`, open the exact paired URL printed in the terminal, and
check that the selected private address is reachable. If an older CLI keeps
loading an empty file, create the first table with the command in the
[CLI quickstart](/getting-started/cli/) or upgrade to a release with empty-file
startup support.

## A command failed in automation

CLI commands emit readable terminal output by default. Automation must pass
`--json` to receive one JSON document on stdout or one JSON error document on
stderr. Failures return a nonzero status in either mode. Preserve stderr and
the exit code; do not parse human terminal text.

## A new record did not receive its Select default

Confirm that the field is Select—not Multi-select—and that its **Default
option** still names a configured option. Defaults apply only when an ordinary
create action omits the field. Explicitly clearing it, importing CSV, or looking
at an existing record does not trigger backfill.

## A record moved or disappeared after editing

Check the active View's filters and sorts. Editing a sort value can move the row
to a new position. Editing a value used by the filter can make the record leave
the View entirely. Clear temporary search or open an unfiltered View to confirm
the record still exists.

## A relative date filter returns an unexpected boundary

Check whether you chose Past, Future, or Current and whether the unit is day,
week, month, or year. Current weeks use Monday through Sunday. Between, on-or-
before, and on-or-after include the selected endpoint. Saved relative filters
recalculate on a new query instead of preserving yesterday's preview dates.

## CSV did not preserve a Relation or attachment

CSV is flat exchange data. Relation labels and attachment names are readable
exports, not complete stable IDs or File entries. Keep or transfer the `.eidos`
file when those semantics must survive.

If the problem remains, include the CLI version, operating system, validation
output, and minimal reproduction in a
[GitHub issue](https://github.com/mayneyao/eidos/issues).
