---
title: Build a Markdown task counter
description: Build, install, and use a practical file context-menu extension from start to finish.
sidebar:
  order: 2
  badge:
    text: Preview
    variant: caution
banner:
  content: "<strong>Runtime preview:</strong> the current Desktop development tree runs trusted command/menu extensions in isolated lazy Workers with read-only text access and host-owned semantic UI."
---

This guide builds **Markdown Task Counter**, a small but useful extension for
people who keep tasks in Markdown notes.

## The user scenario

Suppose `projects/website.md` contains:

```md
# Website launch

- [x] Choose a domain
- [ ] Finish the landing page
- [ ] Invite beta users
```

After installing the extension, the user can:

1. find `website.md` in the Files sidebar;
2. right-click the file;
3. select **Count Markdown tasks**;
4. see `projects/website.md: 2 open, 1 completed` in an Eidos notice.

The extension never modifies the note and never uses the network. It requests
read access to Markdown files only.

## What happens when the command runs

```text
User right-clicks website.md
          │
          ▼
Eidos matches `resourceExtname == .md`
          │
          ▼
User chooses “Count Markdown tasks”
          │ lazy activation
          ▼
Extension worker reads this file through context.space.files
          │
          ▼
Extension counts `[ ]` and `[x]` task-list items
          │
          ▼
Eidos displays a host notice with the result
```

This flow demonstrates the three parts every command extension needs:

- a **manifest contribution** so Eidos can discover the command;
- an **activation handler** containing the behavior;
- a **permission declaration** covering the host operation.

## Step 1: Create the package

Create the following directory outside your production Space while developing:

```text
markdown-task-counter/
├── extension.json
└── src/
    └── extension.ts
```

The complete version used by these docs is available at
[`apps/docs/examples/markdown-task-counter`](https://github.com/mayneyao/eidos/tree/dev/apps/docs/examples/markdown-task-counter).

## Step 2: Describe the extension

Create `extension.json`:

```json
{
  "$schema": "https://docs.eidos.space/schemas/extension-manifest.schema.json",
  "manifestVersion": 1,
  "publisher": "example",
  "name": "markdown-task-counter",
  "displayName": "Markdown Task Counter",
  "description": "Count open and completed tasks in a Markdown file.",
  "version": "0.1.0",
  "engines": { "eidos": ">=0.33.0" },
  "entrypoints": { "worker": "src/extension.ts" },
  "contributes": {
    "commands": [
      {
        "id": "example.markdown-task-counter.count-tasks",
        "title": "Count Markdown tasks"
      }
    ],
    "menus": {
      "files/context": [
        {
          "command": "example.markdown-task-counter.count-tasks",
          "when": "resourceExtname == .md"
        }
      ]
    }
  },
  "permissions": {
    "files": { "read": ["**/*.md"], "write": [] },
    "network": []
  }
}
```

The important lines are:

| Manifest value              | Why it exists                                                       |
| --------------------------- | ------------------------------------------------------------------- |
| `publisher` + `name`        | Derives the stable package ID `example.markdown-task-counter`.      |
| `commands`                  | Makes the command searchable without running extension code.        |
| `files/context`             | Places the command in the Files sidebar context menu.               |
| `when`                      | Hides the command for folders, images, and non-Markdown files.      |
| `files.read`                | Lets the host explain and enforce exactly what the worker may read. |
| empty `write` and `network` | Proves the extension does not need these capabilities.              |

## Step 3: Implement the command

Create `src/extension.ts`:

```ts
import type { ExtensionContext } from "@eidos.space/extension-sdk"

export function activate(context: ExtensionContext) {
  context.subscriptions.add(
    context.commands.register(
      "example.markdown-task-counter.count-tasks",
      async (resource) => {
        const text = await context.space.files.readText(resource.path)
        const open = text.match(/^\s*[-*]\s+\[ \]/gim)?.length ?? 0
        const completed = text.match(/^\s*[-*]\s+\[[xX]\]/gim)?.length ?? 0

        context.window.showNotice(
          `${resource.path}: ${open} open, ${completed} completed`
        )
      }
    )
  )
}
```

`activate()` does not run when Eidos starts. The first invocation of the command
creates the package worker. `context.subscriptions` ensures the command handler
is removed when the extension is disabled, updated, or stopped.

The regular expressions intentionally count only Markdown task-list items at
the start of a line. Text such as ``Use `[ ]` as a marker`` is not counted.

## Step 4: Validate the package

From an Eidos repository checkout, run:

```bash
pnpm --filter docs check:examples
```

Expected output:

```text
Validated example.markdown-task-counter
```

The current check verifies that:

- the complete manifest matches the published version 1 JSON Schema;
- command IDs belong to the package;
- menu items refer to declared commands;
- entrypoints are relative paths that exist inside the package.

The installer will perform these checks before any source is trusted or run.

### Create, approve, and run it in the current Desktop development build

The worker runtime is available as a developer preview in the current
development tree. To create a local package without a dialog:

1. Open a file Space in Eidos Desktop.
2. Open **Settings → Extensions** and select **New extension**.
3. Enter a package name such as `task-counter` and select **Create**.
4. Eidos writes `extension.json`, `src/extension.ts`, and `README.md` under
   `.eidos/extensions/local.task-counter/`.
5. Open **Version** to review the three new source files as ordinary Changes.

To inspect the complete Task Counter example instead:

1. Copy the example directory to
   `<your-space>/.eidos/extensions/example.markdown-task-counter/`.
2. Open that file Space in Eidos Desktop.
3. Open **Settings → Extensions** and select **Refresh**.
4. Inspect the package status and diagnostics.

`Untrusted` means that the manifest, compatibility range, entrypoints, imports,
permissions, and package digest passed inspection but no local trust decision
exists. Select **Review** to inspect the exact content digest and every requested
capability. **Trust source**, enablement, and each permission grant are separate
local decisions. Eidos executes the worker only after all three decisions allow
the exact inspected snapshot.

These decisions live in `.eidos/state/extensions.sqlite3`, which is private and
ignored by Version. They are keyed by package ID, content digest, and permission
hash. Editing any package file creates a different snapshot that returns to
`Untrusted` with enablement off and all grants denied. Stale scans cannot replace
a newer result.

The checked-in example targets Eidos 0.33 and can run in the current Desktop
development build.

## Step 5: Review and record local approval

For a package already present under `.eidos/extensions`:

1. Review its tracked source files in **Version**.
2. Open **Settings → Extensions**, select **Review**, and compare the package ID,
   content digest, and requested capabilities.
3. Select **Trust source**. This approves only the exact inspected snapshot.
4. Grant `files.read **/*.md`. Other capabilities remain denied.
5. Turn on **Enablement** to record local intent for this snapshot.

The status now becomes `Enabled`. Open the command palette and select
**Count Markdown tasks**, or right-click a `.md` file and select the same
command. Eidos lazily starts the package Worker, reads only the selected file
through the granted `files.read` pattern, and shows a host-owned notification
such as `tasks.md: 2 open, 1 completed`.

The extension does not render that notification itself. It requests
`window.showNotice`; Eidos validates the active snapshot and renders its own UI.
The same boundary provides host-owned confirm and select controls. Revoke trust,
disable the package, remove the grant, or edit any source file to terminate the
active runtime and invalidate the old snapshot.

### Install the example from GitHub

GitHub installation is available as a Desktop developer preview. The installer
expects the extension at the repository root, while the checked-in example is a
subdirectory of the Eidos monorepo. To publish this example, create a separate
repository whose root is the contents of
`apps/docs/examples/markdown-task-counter`, then push a tag such as `v0.1.0`.

To install that published package:

1. Open **Settings → Extensions**.
2. Select **Install from GitHub**, paste the public repository URL, and enter
   the release tag.
3. Select **Prepare review**, then review the resolved commit, package ID,
   changed files, and requested
   permission: **Read Markdown files in this Space**.
4. Select **Install reviewed source**. Eidos copies an immutable source snapshot into
   `.eidos/extensions/example.markdown-task-counter/`.
5. Review trust, grants, and enablement. Installation alone never runs the
   package.

The preview supports public repositories only. Eidos does not run `npm install`
or repository scripts, and the publisher repository must not contain the
host-managed `extension.lock.json`.

## Step 6: Use the extension

Once the runtime is available and the extension is enabled:

1. Create or open a Markdown file containing `- [ ]` and `- [x]` tasks.
2. In the **Files** sidebar, right-click that file.
3. Confirm that **Count Markdown tasks** appears. It should not appear on a
   folder, image, `.base`, or other non-Markdown file.
4. Select the command.
5. Confirm the notice reports the expected open and completed counts.

Try these edge cases:

| File content               | Expected result       |
| -------------------------- | --------------------- |
| No task-list items         | `0 open, 0 completed` |
| `* [ ] Task`               | Counted as open       |
| `- [X] Task`               | Counted as completed  |
| ``Text containing `[ ]` `` | Not counted           |

## Step 7: Change and debug it

During local development, changing any installed package file other than the
host-managed `extension.lock.json` creates a new content digest. Eidos stops the
old worker and requires trust for the new digest before reload.
The Extension Manager should show manifest, build, activation, and permission
errors against this package instead of silently retaining the previous bundle.

| Symptom            | Check                                                                      |
| ------------------ | -------------------------------------------------------------------------- |
| Command is missing | Confirm the file ends in `.md` and the menu command ID matches `commands`. |
| Permission error   | Confirm `files.read` includes the selected relative path.                  |
| Activation error   | Open the package diagnostics and inspect `src/extension.ts`.               |
| Counts are wrong   | Add the failing Markdown line to a test before changing the parser.        |

## Next steps

- [Manifest](/extensions/manifest/) explains every package declaration.
- [Lifecycle](/extensions/lifecycle/) covers activation, source changes, and failures.
- [Security](/extensions/security/) covers trust and capability enforcement.
- [GitHub distribution](/extensions/distribution/) covers publishing and updates.
