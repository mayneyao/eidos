---
title: CLI 快速开始
description: 安装 Eidos CLI，创建一个可用文件，并打开本地编辑器。
sidebar:
  order: 30
---

import { Aside, TabItem, Tabs } from "@astrojs/starlight/components"

独立的 `eidos` CLI 直接操作 `.eidos` 文件，不依赖 Eidos Lite 或后台服务。

## 1. 安装

<Tabs syncKey="cli-install">
  <TabItem label="macOS / Linux">

```bash
curl -fsSL https://download.eidos.space/cli/install.sh | sh
```

  </TabItem>
  <TabItem label="Windows PowerShell">

```powershell
irm https://download.eidos.space/cli/install.ps1 | iex
```

  </TabItem>
</Tabs>

如果安装后 `eidos` 没有立即进入 `PATH`，请重启终端，然后运行 `eidos --help`。

## 2. 创建带数据表的文件

```bash
eidos create example.eidos \
  --table Tasks \
  --label-field Title \
  --fields '[{"name":"Title","type":"text"},{"name":"Status","type":"select"}]'
```

CLI 会把这张初始表设为文件默认表，并创建第一个 Grid 视图。字段默认允许空值；只有高级
Schema 操作才需要显式配置非空约束。

## 3. 打开本地编辑器

```bash
eidos serve example.eidos --open
```

服务默认监听 `127.0.0.1`，打开共享 Eidos File UI，并把已提交的编辑直接写入
`example.eidos`。要在可信私有网络的另一台设备上使用，可运行
`eidos serve example.eidos --lan` 并打开终端输出的配对 URL。按 <kbd>Ctrl</kbd> +

<kbd>C</kbd> 停止服务。

<Aside type="tip" title="校验结果">

```bash
eidos example.eidos validate --level full
```

</Aside>

下一步阅读 [CLI 与自动化指南](/zh-cn/cli/)。需要稳定、只读的托管 URL 时，继续阅读
[Eidos Publish 预览指南](/zh-cn/cli/publish/)；需要持续连接本机文件的实时编辑器时，使用
[`eidos serve`](/zh-cn/cli/serve/)。
