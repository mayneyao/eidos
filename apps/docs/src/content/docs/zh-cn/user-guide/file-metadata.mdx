---
title: 文件元数据管理
description: 直接在当前文件夹中管理本地文件的自定义属性，多级文件夹各自独立又共享底层属性。
sidebar:
  label: 文件元数据管理
  order: 60
---

import { FileTree } from "@astrojs/starlight/components"

在日常整理本地的视频、设计图稿、摄影素材或合同文件时，系统的文件管理器通常只能展示文件名与修改时间，无法直接给文件添加标签、评分、记录审核状态或备忘。

**文件元数据管理**可以将一个普通的本地文件夹以结构化表格的形式打开。你在表格中记录的每一项自定义属性，都会直接保存在物理文件本身上，让你轻松对本地数字资产进行分类、打标与筛选。

---

## 1. 快速上手

开启文件元数据管理只需三步：

1. **新建文件**：在 Eidos Lite 侧边栏中点击新建文件（快捷键 `Cmd/Ctrl + N`），文件类型选择 **Eidos**。
2. **开启开关**：勾选底部的 **文件元数据管理** 开关，文件名为默认的 `files.eidos`（也可重命名为你喜欢的名称），点击**创建**。
3. **开始打标**：打开创建的表格后，点击列头右侧的 **+（添加列）**，输入你要记录的属性名称（如“审核状态”、“星级”或“分类标签”），即可像填写普通表格一样直接为文件录入信息。

---

## 2. 日常使用与文件安全

- **原文件零拷贝，安全无损**：你的所有物理文件依然留在原磁盘目录中。Eidos 既不会生成重复的副本浪费磁盘空间，也不会修改文件本体的原始内容。
- **自动感知磁盘变动**：当你在操作系统中添加、重命名或删除了某个文件，表格会自动感知并实时更新，无需手动刷新。
- **即时全屏预览**：点击记录中的文件或附件图标，即可随时呼出大图或文档预览，无需离开当前工作界面。

---

## 3. 管理范围与作用域

**一个开启了元数据管理的 `.eidos` 文件，只管理它当前所属的那一个文件夹。**

它不会跨目录扫描全盘，每一个文件都拥有清晰明确的本地边界：当前所在的目录就是它的全部管理视界。

<FileTree>

- 电影资料库/
  - **files.eidos** 仅管理当前文件夹下的文件
  - 流浪地球2.mp4
  - 星际穿越.mkv
  - 电影海报.jpg
  - 影视说明.pdf

</FileTree>

---

## 4. 多级文件夹与属性共用机制

在复杂的项目目录中，经常会有多层子文件夹（例如大项目下包含专属的素材子目录）。当父文件夹与子文件夹各自创建了 `files.eidos` 时：

<FileTree>

- 创意项目库/
  - **files.eidos** 管理当前根目录文件
  - 项目规划.pdf
  - 预算明细.xlsx
  - 视觉设计/
    - **files.eidos** 独立管理子目录
    - 主视觉海报.png
    - 矢量标志.svg
    - 产品插画.jpg
  - 参考资料/
    - 竞品分析.md
    - 灵感合辑.png

</FileTree>

### 独立排版，互不干扰
父级文件夹下的 `.eidos` 与子文件夹下的 `.eidos` 是**各自完全独立**的文件：
两边的表格排版、视图模式、过滤规则、排序条件完全隔离，互不影响。子文件夹没有创建 `.eidos` 时，也不受任何影响。

### 物理文件公用属性字段
由于自定义属性是直接记录在具体物理文件自身的扩展属性中：
- **同名字段读写同一数据**：只要两级表格中存在**相同名称的字段**（例如都叫 `tags`、`rating` 或 `status`），它们实际读写的都是对应文件底层绑定的同一份属性。
- **双向联动更新**：在子文件夹表格中给某个文件打了星级评分，在父文件夹表格中查看该文件时，这个评分同样会实时可见。

### 必须了解的注意事项

1. **同名字段的修改是实时的**：对于同一个文件，在任一表格中修改其属性，本质上改动的是文件本身的系统属性，所有包含该文件的视图都会同步反映最新值。
2. **移动文件完整继承属性**：将文件从一个文件夹移动到另一个文件夹时，文件自带的元属性不会丢失。只要目标文件夹的表格也包含同名字段，就能立即识别并显示该文件已有的元属性。
3. **“删除字段”与“清空单元格”的区别**：
   - 在表格中**删除某一列（字段）**，只是在当前表格中移除了该列的排版定义，**并不会清空文件本身已保存的数据**；
   - 只有当你在表格中手动**把单元格内容清空**时，才会真正从对应文件的系统元属性中抹掉该属性值。
4. **属性随身携带的范围**：
   - 文件元属性保存在现代主流文件系统（macOS APFS、Linux ext4、Windows NTFS）的扩展属性中。日常在本地磁盘、移动固态硬盘（SSD）以及通过 Graft 进行版本控制与同步时，元数据均能完整稳定保留。
   - 若将文件单独通过某些不支持扩展属性的第三方纯网络工具传输（如微信传输单个文件）或拷入精简格式的 FAT32 介质，附加属性可能会被系统忽略。

---

## 5. 系统内置只读字段

表格会自动扫描并提供以下底层物理属性（系统只读，无法手动覆写）：

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `name` | 文本 | 物理文件名（如 `Photo.jpg`） |
| `path` | 文本 | 文件在当前文件夹内的相对路径，作为记录标题与唯一标识 |
| `extension` | 文本 / 标签 | 文件后缀名（如 `jpg`、`mp4`、`pdf`） |
| `size` | 文本 / 整数 | 文件大小（自动格式化为易读的 KB、MB、GB） |
| `mimetype` | 文本 | 媒体格式类型（如 `image/jpeg`、`video/mp4`），便于一键筛选同类媒体 |
| `file` | 附件引用 | 本地文件实体引用，用于卡片缩略图生成与点击全屏预览 |
