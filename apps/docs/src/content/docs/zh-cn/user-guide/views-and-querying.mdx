---
title: 视图、搜索、筛选与排序
description: 在同一组记录上建立聚焦的 Grid、Gallery、Kanban 与 Calendar 工作流。
sidebar:
  order: 40
---

视图是一张数据表的保存阅读与工作方式，不会复制记录。在一个视图中修改值，所有视图都会看到
同一条记录的变化。

## 视图会保存什么

视图可以保存名称、类型、筛选、排序、可见字段、字段顺序和类型专用布局。临时搜索词、选择、
滚动位置、hover 状态和打开的详情面板不会保存进去。

不同任务应建立不同视图，例如**全部任务**、**我的未完成工作**、**本周到期**和**归档**，
而不是每天重建同一个复杂筛选。

## 选择 Grid、Gallery、Kanban 或 Calendar

| 视图     | 适合的工作                     |
| -------- | ------------------------------ |
| Grid     | 密集录入、比较、排序和字段操作 |
| Gallery  | 用封面与关键字段浏览卡片       |
| Kanban   | 在单选状态等分组之间移动记录   |
| Calendar | 按日期字段在月历中规划记录     |

切换视图类型或卡片布局只改变展示。Kanban 移动可能更新配置的分组字段，因为移动卡片本身表达
了一次数据修改。

Calendar 需要日期、日期时间、兼容的 Formula/Lookup，或创建/更新时间系统字段。新建
Calendar 时会选择第一个可用字段；可在**配置视图 → 日期字段**中更换。**今天**回到本月，
左右箭头按月切换。搜索与已保存筛选仍然生效。日期时间按当前本地时区归入日期，纯日期则保留
保存的准确日期。配置字段为空的记录不会出现在日历中。点击日历项目会打开与其他视图相同的
记录详情面板。选择可写的日期或日期时间字段时，可以使用任意日期格中的新建操作，记录会自动
填入该日期。选择**创建时间**或**更新时间**时，只能在今天新建，因为时间戳由 Runtime
维护。Formula 和 Lookup 日期是派生值，不能从日期格新建记录。Eidos Lite 默认从周一
开始显示每周；如果要从周日开始，可关闭**设置 → 偏好设置 → 每周从周一开始**。这是全局展示
偏好，不会保存到视图或 Eidos File 中。

## 临时搜索

搜索会在受支持字段和记录标题的可展示片段中匹配。它只缩小当前视图，不会改变保存筛选。清空
搜索框即可回到视图的完整保存结果。

已知标题、选项、关联记录标签或文件名的一部分时使用搜索；明天仍应生效的条件使用保存筛选。

## 建立筛选组

打开**筛选**并添加条件。**全部匹配**表示 AND，**任一匹配**表示 OR。嵌套组可以表达：

> 状态不是已完成 AND（优先级是高 OR 截止日期相对本周）

空值和否定条件使用完整 Boolean 逻辑。“不是”与“不包含”可以包含空的 scalar 值，不会泄漏
SQL 的第三种 unknown truth value。

## 日期与时间条件

日期和日期时间字段提供：

- **是**
- **早于** / **晚于**
- **不晚于** / **不早于**
- **介于**两个包含首尾的端点
- **相对今天**
- **为空** / **不为空**

相对条件组合一个方向和单位：

| 方向 | 含义                                      |
| ---- | ----------------------------------------- |
| 过去 | 截止到 reference 的一个滚动日、周、月或年 |
| 未来 | 从 reference 开始的一个滚动日、周、月或年 |
| 本期 | 包含今天的 calendar 日、ISO 周、月或年    |

**本期周**使用周一到周日。移动月或年时会 clamp 月末日期，因此闰年 2 月 29 日的一年后在
需要时会落到 2 月 28 日。保存的是方向与单位，新查询会重新计算；日历预览中的日期不会被
冻结到视图里。

一次查询只捕获一个 reference instant。所有相对条件和 continuation page 使用同一个值，
因此时间前进时，记录不会在分页过程中跳动。

## 添加多级排序

排序按顺序计算：第一个是主排序，第二个用于处理相同值，依此类推。排序方向只改变行顺序，
不会改变记录。

如果编辑改变了排序字段，行可能移动。它仍在视图内时，编辑器可以跳到新位置；如果不再满足
筛选，它会离开视图，因为保存查询已经不成立。

## 选择可见字段

打开**字段**可以：

- 在当前视图显示或隐藏字段；
- 拖动字段改变视图顺序；
- 直接打开字段属性，而不用寻找列菜单。

隐藏字段不会删除数据。Grid 还可以冻结前部区域并保存字段宽度；Gallery 与 Kanban 保存各自
适用的卡片字段顺序、封面和其他布局属性。Calendar 只保存选中的日期字段，当前浏览月份是
临时状态。

## 示例视图

### 本周到期

- 状态**不是**已完成
- 截止日期**相对今天 → 本期 → 周**
- 截止日期升序，然后优先级降序

### 最近活跃

- 更新时间**相对今天 → 过去 → 周**
- 更新时间降序

### 指定日程区间

- 开始日期**介于**选择的开始与结束日期
- 开始日期升序

## 理解保存状态

应用筛选、排序或字段设置会修改视图，因此会改变 File revision。输入临时搜索词不会。如果
另一编辑器先修改了同一视图，Eidos 会刷新，而不是静默覆盖较新的定义。

## 在不同 Eidos 版本间协作

较新 Eidos 版本创建的视图，在较旧但支持兼容降级的客户端中仍然可见。如果该客户端不理解
视图 renderer、筛选或排序规则，它会显示需要更新的状态，不会隐藏视图，也不会把未知筛选
当作“没有筛选”。这样可以避免展示错误的记录集合。

原始配置会继续保留在文件中。重命名、调整视图顺序或修改无关布局，不会重写更新版本的
query。更新 Eidos 后即可正常使用该视图；只有显式替换不受支持的筛选或排序，才会移除较新
版本的 query 定义。

需要与其他系统交换数据时，继续阅读[导入与导出](/zh-cn/user-guide/import-and-export/)。
